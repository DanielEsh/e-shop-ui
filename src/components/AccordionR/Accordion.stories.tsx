import React from 'react';
import { Story, Meta } from '@storybook/react';

import {AccordionR} from './index';

export default {
    title: 'R',
    component: AccordionR,
} as Meta;

export const Primary = () => <AccordionR />;
