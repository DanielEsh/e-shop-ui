import React from 'react';
import { Dropdown } from "../Dropdown";

const Default = (): JSX.Element => {
    return (
        <>
            <Dropdown>
                dropdown element
            </Dropdown>
        </>
    );
};

export default Default;
