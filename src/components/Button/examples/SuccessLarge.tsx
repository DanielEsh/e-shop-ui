import React from 'react';
import {Button} from "../index";

const SuccessLarge = (): JSX.Element => {
    return (
        <Button
            color="success"
            outline
            size="large"
        >
            Success button
        </Button>
    );
};

export default SuccessLarge;
