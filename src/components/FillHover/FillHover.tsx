import { useState } from 'react'

const defaultCoords = {
  x: 50,
  y: 50,
}

export const FillHover = () => {
  const [hoverPosition, setHoverPosition] = useState(defaultCoords)

  const onHover = () => {
    // get mouse position
    // get hover position
  }

  return (
    <div />
  )
}
